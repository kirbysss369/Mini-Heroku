# Infrastructure

This directory is reserved for infrastructure configuration introduced by later milestones.

For the bootstrap milestone, PostgreSQL is defined directly in the root `docker-compose.yml`. No additional infrastructure is configured here yet.
